# Harness

Shared code every feature builds on instead of reinventing: trajectory
logging, the eval runner (golden tasks + oracle, reports pass/fail and cost),
and the base loop (retry policy, budget guards, stop rules).
