# fhir-mcp

The MCP server standing between agents and the HAPI FHIR server. Design isn't
locked yet, three tool levels are planned (thin passthrough, task-shaped
tools, code mode) and the internal layout will change as that gets built. This
folder is the placeholder for it; treat what's here now as a starting slot,
not a spec.
