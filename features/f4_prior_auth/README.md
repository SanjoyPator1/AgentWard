# F4 — Prior Authorisation Agent

The write path. Checks a proposed order against a coverage policy, then
either creates the authorisation resource or refuses with a cited clause. The
oracle checks the answer and the resulting server state.
