# F3 — Cohort Query Agent

Free-text cohort questions FHIR search can't express directly (no joins, no
"not currently prescribed"). Forces real multi-step planning: search, fetch,
paginate, filter in code.
