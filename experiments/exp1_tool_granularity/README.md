# Experiment 1 — Tool Granularity

Same task set run against Level 1, Level 2, and Level 3 tools in fhir-mcp.
Records tool calls, tokens, wall clock, accuracy, and which tasks each level
makes impossible.
